import React from "react";

const Layout = () => (
    <html>
        <head>
            <title>Layout</title>
        </head>
        <body>
            <p>Home</p>
        </body>
    </html>
);

export default Layout;
