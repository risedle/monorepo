{
    "name": "@subgraphs/uniswap-v3",
    "private": true,
    "scripts": {
        "codegen": "graph codegen",
        "graph:build": "graph build",
        "graph:deploy": "graph deploy --node https://api.thegraph.com/deploy/ risedle/uniswap-v3",
        "create-local": "graph create --node http://localhost:8020/ risedle/uniswap-v3",
        "remove-local": "graph remove --node http://localhost:8020/ risedle/uniswap-v3",
        "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 risedle/uniswap-v3",
        "graph:test": "graph test"
    },
    "dependencies": {
        "@graphprotocol/graph-cli": "0.33.0",
        "@graphprotocol/graph-ts": "0.27.0"
    },
    "devDependencies": {
        "matchstick-as": "0.5.0"
    }
}
